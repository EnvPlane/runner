name: Runner CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      # EnvPlane runtime modules are public. PR validation must remain
      # reproducible without release credentials; GitHub App authentication is
      # reserved for publication workflows that write packages or repositories.
      - name: Enforce schema ownership
        run: |
          test ! -d migrations/postgres
          test ! -f internal/server/openapi.json
          test ! -d internal/postgres
          test ! -f internal/gitops/renderer.go
      - name: Ensure Go toolchain
        run: |
          GO_VERSION_FILE=go.mod \
          ./scripts/ensure-go.sh
      - name: Select module Go toolchain
        run: echo "GOTOOLCHAIN=go$(awk '/^go[[:space:]]/ {print $2; exit}' go.mod)" >> "$GITHUB_ENV"
      - name: Cache Go modules and build cache
        uses: actions/cache@v4
        with:
          path: |
            ~/go/pkg/mod
            ~/.cache/go-build
          key: runner-go-cache-${{ runner.os }}-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            runner-go-cache-${{ runner.os }}-
      - name: Use published runtime modules
        run: |
          go mod edit -dropreplace=github.com/envplane/contracts
          go mod edit -dropreplace=github.com/envplane/gitops
          go mod tidy
      - name: Scan Go dependencies
        run: go run golang.org/x/vuln/cmd/govulncheck@v1.7.0 ./...
      - run: go test ./...
      - name: Verify production package reachability
        run: ./scripts/check-production-reachability.sh
      - run: go vet ./...
      - name: Lint Go code
        run: scripts/golangci-lint.sh
      - run: go test -race ./...
      - run: go test -coverprofile=cover.out ./...
      - name: Enforce coverage floor
        run: |
          total="$(go tool cover -func=cover.out | awk '/^total:/ {gsub(/%/, "", $NF); print $NF}')"
          awk -v total="$total" 'BEGIN { if (total < 20) { print "coverage below 20%: " total "%"; exit 1 } }'
